---
title: Cross-Modal Reinforcement Learning for Navigation with Degraded Depth Measurements
title_zh: 针对深度测量退化情况下的跨模态强化学习导航
authors: "Omkar Sawant, Luca Zanatta, Grzegorz Malczyk, Kostas Alexis"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22182v1"
tags: ["query:rgb-d"]
score: 8.0
evidence: 深度图与灰度图之间的跨模态学习
tldr: 针对深度传感器在恶劣光照或反射表面下失效的问题，本文提出一种跨模态强化学习框架。通过跨模态 Wasserstein 自动编码器（CMWAE）学习深度与灰度图像的共享潜空间表示，使系统能在深度测量退化时利用灰度图推断深度特征。结合强化学习策略，实现了在非结构化环境中的鲁棒避障导航，并在实机实验中验证了其有效性。
selection_source: fresh_fetch
motivation: 旨在解决深度传感器在复杂环境（如强光、反射）中因测量失效而导致机器人导航性能下降的问题。
method: 提出一种跨模态 Wasserstein 自动编码器来学习深度与灰度图的共享表示，并将其集成到强化学习策略中。
result: 实验证明该方法在深度信息严重退化的情况下仍能保持鲁棒的导航表现，并具备良好的跨场景迁移能力。
conclusion: 通过跨模态一致性学习互补特征，可以显著提升机器人在传感器受损环境下的自主导航可靠性。
---

## 摘要
本文提出了一种跨模态学习框架，利用深度图像和灰度图像的互补信息来实现鲁棒导航。我们引入了一种跨模态 Wasserstein 自动编码器（Cross-Modal Wasserstein Autoencoder），通过强制跨模态一致性来学习共享的潜在表示，使系统能够在深度测量受损时从灰度观测中推断出与深度相关的特征。学习到的表示与基于强化学习的策略相结合，用于在光照不足或反射表面等不利条件下深度传感器发生退化时，在非结构化环境中实现无碰撞导航。仿真和真实世界实验表明，我们的方法在深度显著退化的情况下仍能保持鲁棒性能，并成功迁移到真实环境中。

## Abstract
This paper presents a cross-modal learning framework that exploits complementary information from depth and grayscale images for robust navigation. We introduce a Cross-Modal Wasserstein Autoencoder that learns shared latent representations by enforcing cross-modal consistency, enabling the system to infer depth-relevant features from grayscale observations when depth measurements are corrupted. The learned representations are integrated with a Reinforcement Learning-based policy for collision-free navigation in unstructured environments when depth sensors experience degradation due to adverse conditions such as poor lighting or reflective surfaces. Simulation and real-world experiments demonstrate that our approach maintains robust performance under significant depth degradation and successfully transfers to real environments.

---

## 论文详细总结（自动生成）

### 针对深度测量退化情况下的跨模态强化学习导航论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：自主机器人在复杂环境（如地下隧道、灾难现场）导航时，深度传感器（如RGB-D相机）常因强光、反射表面或低纹理区域导致测量数据退化或丢失，进而引发导航失效。
*   **研究动机**：传统的导航方法（无论是模块化还是端到端）在深度信息受损时缺乏有效的备选策略。然而，即使深度图失效，灰度或RGB图像通常仍能提供关键的结构信息。本文旨在通过跨模态学习，利用灰度图补全深度特征，提升机器人在传感器受损环境下的避障鲁棒性。

#### 2. 方法论
*   **核心思想**：构建一个共享的潜在空间（Latent Space），通过跨模态一致性训练，使编码器能够从灰度图像中推断出缺失的深度特征。
*   **关键技术细节**：
    *   **跨模态 Wasserstein 自动编码器 (CMWAE)**：
        *   **架构**：包含两个独立的编码器分支（基于 AlexNet），分别处理灰度图和深度图；通过共享投影网络生成融合的潜在向量 $z$；最后由解码器尝试重建深度图。
        *   **损失函数**：结合了重建损失（$L_{rec}$）和基于最大均值差异（MMD）的正则化项，利用 Wasserstein 距离确保潜在空间的分布一致性与泛化能力。
        *   **训练策略**：在训练过程中对深度图进行随机遮挡（Corruption），强制网络学习如何从完好的灰度图中提取几何特征来补偿深度损失。
    *   **强化学习导航策略**：
        *   **算法**：采用近端策略优化（PPO）。
        *   **观测空间**：包括 CMWAE 生成的潜在向量 $z$、机器人状态（速度、姿态、角速度）、目标相对位置及上一时刻动作。
        *   **动作空间**：输出速度大小、速度倾角和偏航率。
        *   **网络结构**：全连接层结合门控循环单元（GRU），以处理时间序列信息。

#### 3. 实验设计
*   **数据集/场景**：
    *   **感知训练**：使用 TartanAir 数据集（包含同步的 RGB 和深度图）进行 CMWAE 的预训练。
    *   **仿真环境**：在 Aerial Gym 模拟器中构建具有随机障碍物（面板、杆件、板材）的房间，采用课程学习（Curriculum Learning）逐步增加障碍物密度。
    *   **实机实验**：在 28 米长的走廊中进行，通过关闭深度相机的红外投影仪人为制造深度测量退化。
*   **Benchmark 与对比方法**：
    *   对比了 **DCE-RL**（深度碰撞编码强化学习），该方法仅依赖深度信息且未针对数据退化进行训练。

#### 4. 资源与算力
*   **硬件平台**：实机实验使用了定制的四旋翼无人机，搭载 **NVIDIA Jetson Orin NX (16GB)** 进行板载推理，底层控制由 PX4 自动驾驶仪负责。
*   **算力说明**：文中提到使用了高度并行化的 Aerial Gym 模拟器进行训练，但未明确给出具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **感知评估**：在 TartanAir 测试集上对比了两种不同的遮挡方案（S1 和 S2），涵盖 0% 到 50% 的不同退化比例。
    *   **仿真评估**：在 20 个难度等级的课程学习环境中进行了大规模测试，统计了成功率、超时率和碰撞率。
    *   **实机验证**：在 7 种不同的障碍物布局环境下进行了实飞测试。
*   **充分性评价**：实验设计较为全面，涵盖了从离线指标（MSE, SSIM）到闭环控制性能的评估，并验证了从仿真到现实（Sim2Real）的迁移能力，对比实验客观地展示了该方法在极端情况下的优势。

#### 6. 主要结论与发现
*   **跨模态补全有效**：CMWAE 能够有效地从灰度图中提取几何结构，即使深度图丢失 50% 的像素，仍能保持较高的重建质量。
*   **鲁棒性显著提升**：在深度信息完好时，本文方法略逊于纯深度专家模型（因为潜在空间需兼顾两种模态）；但在深度信息退化时，对比方法（DCE-RL）几乎完全失效，而本文方法仍能保持较高的导航成功率。
*   **Sim2Real 成功**：无需在真实世界中进行微调，策略即可直接部署在无人机上，处理真实环境中的传感器失效问题。

#### 7. 优点
*   **互补性利用**：巧妙地利用了灰度图作为深度传感器的“软备份”，解决了单一模态失效的痛点。
*   **潜在空间正则化**：采用 Wasserstein 距离而非传统 VAE 的 KL 散度，获得了更稳定、质量更高的潜在表示。
*   **端到端鲁棒性**：将感知层的抗干扰能力直接转化为控制层的导航鲁棒性。

#### 8. 不足与局限
*   **表示带宽权衡**：由于共享潜在空间，在传感器完全正常的情况下，其性能上限受限于潜在向量的维度分配，导致其在理想工况下表现略逊于专用模型。
*   **遮挡模式单一**：训练中的遮挡主要模拟“块状丢失”，对于其他类型的传感器噪声（如系统性偏差、运动模糊或雨雾干扰）的泛化能力尚待验证。
*   **环境复杂度**：实机实验主要在走廊等相对规则的室内环境中进行，尚未在更复杂的室外动态环境中测试。

（完）
