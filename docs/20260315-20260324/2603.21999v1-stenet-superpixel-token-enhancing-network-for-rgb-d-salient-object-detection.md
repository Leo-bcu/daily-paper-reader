---
title: "STENet: Superpixel Token Enhancing Network for RGB-D Salient Object Detection"
title_zh: STENet：用于 RGB-D 显著目标检测的超像素 Token 增强网络
authors: "Jianlin Chen, Gongyang Li, Zhijiang Zhang, Liang Chang, Dan Zeng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21999v1"
tags: ["query:rgb-d"]
score: 8.0
evidence: 具有跨模态交互的RGB-D显著性目标检测
tldr: 针对RGB-D显著性目标检测中Transformer计算复杂度高和局部细节提取不足的问题，本文提出超像素令牌增强网络（STENet）。该网络采用双流编解码结构，通过改进的超像素生成方法，构建了全局增强模块（建模像素与超像素关系以降低复杂度）和局部精细化模块（利用超像素内相似性提取细节），实现了跨模态特征的高效融合与增强，在多个数据集上表现优异。
selection_source: fresh_fetch
motivation: 现有的基于Transformer的RGB-D显著性检测方法面临注意力机制计算复杂度高以及局部细节提取能力受限的挑战。
method: 提出STENet，通过改进的超像素生成方法构建全局增强和局部精细化模块，分别建模像素到超像素的全局关系和超像素内的局部细节。
result: 在七个主流RGB-D显著性目标检测数据集上的实验结果表明，STENet的性能达到了当前最先进水平。
conclusion: 引入超像素作为令牌能有效降低Transformer的计算开销并提升局部特征表示，为跨模态显著性检测提供了高效的新思路。
---

## 摘要
基于 Transformer 的 RGB-D 显著目标检测 (SOD) 方法因其捕获长距离像素依赖关系的卓越能力而受到广泛关注。然而，当前的 RGB-D SOD 方法面临着挑战，例如注意力机制的二次复杂度和有限的局部细节提取。为了克服这些局限性，我们提出了一种新颖的超像素 Token 增强网络 (STENet)，该网络将超像素引入跨模态交互。STENet 遵循双流编码器-解码器结构。其核心是两个定制的超像素驱动跨模态交互模块，负责全局和局部特征增强。具体而言，我们通过扩展每个超像素的邻域范围来更新超像素生成方法，从而实现像素与超像素之间的灵活转换。利用更新后的超像素生成方法，我们首先提出了超像素注意力全局增强模块，用于建模全局像素到超像素的关系，而非传统的全局像素到像素关系，这可以捕获区域级信息并降低计算复杂度。我们还提出了超像素注意力局部细化模块，该模块利用超像素内的像素相似性来过滤出一组像素子集（即局部像素），然后对这些局部像素进行特征增强，从而捕获相关的局部细节。此外，我们将全局和局部增强的特征与跨尺度特征融合，以实现全面的特征表示。在七个 RGB-D SOD 数据集上的实验表明，与最先进的方法相比，我们的 STENet 取得了具有竞争力的性能。我们的代码和结果可在 https://github.com/Mark9010/STENet 获取。

## Abstract
Transformer-based methods for RGB-D Salient Object Detection (SOD) have gained significant interest, owing to the transformer's exceptional capacity to capture long-range pixel dependencies. Nevertheless, current RGB-D SOD methods face challenges, such as the quadratic complexity of the attention mechanism and the limited local detail extraction. To overcome these limitations, we propose a novel Superpixel Token Enhancing Network (STENet), which introduces superpixels into cross-modal interaction. STENet follows the two-stream encoder-decoder structure. Its cores are two tailored superpixel-driven cross-modal interaction modules, responsible for global and local feature enhancement. Specifically, we update the superpixel generation method by expanding the neighborhood range of each superpixel, allowing for flexible transformation between pixels and superpixels. With the updated superpixel generation method, we first propose the Superpixel Attention Global Enhancing Module to model the global pixel-to-superpixel relationship rather than the traditional global pixel-to-pixel relationship, which can capture region-level information and reduce computational complexity. We also propose the Superpixel Attention Local Refining Module, which leverages pixel similarity within superpixels to filter out a subset of pixels (i.e., local pixels) and then performs feature enhancement on these local pixels, thereby capturing concerned local details. Furthermore, we fuse the globally and locally enhanced features along with the cross-scale features to achieve comprehensive feature representation. Experiments on seven RGB-D SOD datasets reveal that our STENet achieves competitive performance compared to state-of-the-art methods. The code and results of our method are available at https://github.com/Mark9010/STENet.

---

## 论文详细总结（自动生成）

### STENet：用于 RGB-D 显著目标检测的超像素 Token 增强网络论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 Transformer 的 RGB-D 显著目标检测（SOD）方法虽然擅长捕获长程依赖，但面临**计算复杂度高（二次方增长）**和**局部细节提取不足**的双重挑战。
*   **研究动机**：
    *   传统的 CNN 缺乏全局上下文建模能力。
    *   Transformer 在处理高分辨率图像时计算开销巨大，且往往忽略了细微的局部结构。
    *   **超像素（Superpixel）**能将像素聚类为几何一致的单元，既能保留结构信息又能降低冗余。本文旨在将超像素引入 Transformer 架构，以实现高效且精准的跨模态特征增强。

#### 2. 方法论
STENet 采用双流编码器-解码器结构，核心创新在于利用超像素驱动的交互模块：
*   **改进的超像素生成**：
    *   基于局部交叉注意力机制，将像素聚类为超像素 Token。
    *   **关键改进**：将超像素的搜索邻域从传统的 3×3 扩展到 **5×5**，允许像素在更广的范围内寻找相似的超像素，增强了语义判别力。
*   **超像素注意力全局增强模块 (SAGEM)**：
    *   **核心思想**：建模“像素-到-超像素”的全局关系，而非传统的“像素-到-像素”。
    *   **流程**：通过跨模态超像素交互生成共享注意力图，将全局上下文信息从超像素空间映射回像素空间，显著降低了空间维度的计算复杂度。
*   **超像素注意力局部精细化模块 (SALRM)**：
    *   **核心思想**：利用超像素内部的相似性过滤出关键局部像素。
    *   **流程**：在每个超像素内选取 Top-K 个最相似的像素，通过交叉注意力机制进行精细化增强，专门用于修复被全局模块忽略的边缘和纹理细节。
*   **特征融合**：将全局增强特征、局部精细化特征与跨尺度特征进行级联与自注意力融合，生成最终的显著性图。

#### 3. 实验设计
*   **数据集**：在 7 个主流 RGB-D SOD 数据集上进行了验证，包括 **NJUD、NLPR、LFSD、SIP、SSD、STEREO 和 DUT-RGBD**。
*   **Benchmark 与对比方法**：
    *   对比了 14 种最先进（SOTA）的方法。
    *   涵盖了以 **ResNet-50** 为骨干的网络（如 DSNet, DCFNet, CAVER 等）和以 **Swin-B** 为骨干的网络（如 SwinNet, CATNet, CPNet 等）。
*   **评价指标**：采用 MAE ($M$)、E-measure ($E_\xi$)、S-measure ($S_m$)、F-measure ($F_\beta$) 和 Weighted F-measure ($F_\beta^\omega$)。

#### 4. 资源与算力
*   **硬件环境**：使用单块 **NVIDIA GTX 3090 GPU (24GB RAM)**。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：
    *   优化器：Adam，采用余弦退火学习率调度。
    *   参数设置：学习率 5e-5，训练 100 个 epoch。
    *   Batch Size：Swin-B 版本为 5，ResNet-50 版本为 24。
    *   输入尺寸：384×384（Swin-B）或 256×256（ResNet-50）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在 7 个数据集上进行了详尽的性能测试。
    *   **定性对比**：展示了包括小物体、多物体、复杂背景、深度图不可靠、低光照等多种挑战场景的可视化结果。
    *   **消融实验**：共进行了 7 组核心消融实验，验证了 SAGEM、SALRM 的有效性，探讨了超像素尺寸配置、采样方法（对比 K-means 等）以及掩码尺寸（3x3 vs 5x5）的影响。
*   **充分性评价**：实验设计非常全面，不仅对比了精度，还对比了**参数量（Params）和计算量（FLOPs）**，证明了模型在效率与精度之间的平衡，实验过程客观公平。

#### 6. 主要结论与发现
*   **性能领先**：STENet 在多个指标上超越了现有的 Transformer 方法，尤其是在复杂场景（如 SIP 数据集）中表现突出。
*   **超像素的价值**：引入超像素作为 Token 能够有效打破 Transformer 的计算瓶颈，同时通过局部精细化弥补了聚类带来的信息损失。
*   **邻域扩展的必要性**：实验证明 5×5 的邻域比 3×3 更有助于在高分辨率特征图中捕获准确的语义关联。

#### 7. 优点与亮点
*   **高效性**：相比 CPNet 等方法，STENet 在保持高性能的同时，计算量（FLOPs）降低了约 16.6%，参数量也更少。
*   **双重增强机制**：SAGEM 负责全局轮廓，SALRM 负责局部细节，这种“粗到细”的设计逻辑清晰且互补。
*   **鲁棒性**：在深度图质量较差或低对比度环境下，通过跨模态超像素交互，模型仍能较好地定位目标。

#### 8. 不足与局限
*   **深度图依赖风险**：当深度图存在严重噪声时，模型仍可能误将背景区域识别为显著物体。
*   **弱纹理场景限制**：在纹理极弱的场景下，局部特征相似性可能导致超像素跨越语义边界，造成目标合并或边缘模糊。
*   **应用限制**：虽然降低了复杂度，但超像素生成过程仍涉及多次迭代（文中设为 2 次），在极高性能要求的实时场景中可能仍有优化空间。

（完）
